package com.neel.infyassignment.domain.model

/**
 * UI model type used only by the domain layer.
 *
 *
 * In this package there can be many different domain models
 * that are used by ui layer and later it will be mapped to ui states
 *
 *
 * A separate mapper will be used to convert
 * domain models into these UI models.
 */
class DomainModel