package com.neel.infyassignment.ui.pois.state

/**
 * UI states type used only by the UI layer.
 * <p>
 * In this package there can be many different UI states
 * that are shaped for the screen needs.
 * <p>
 * A separate mapper will be used to convert
 * domain models into these UI states.
 */
class UiState {
}